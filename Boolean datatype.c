#include <stdio.h>
#include <stdbool.h>

int main() {
    bool isRaining = true;
    if (isRaining)
        printf("Take an umbrella!\n");
    else
        printf("Enjoy the sunshine!\n");
    return 0;
}
